<div class="fixed-action-btn">
  <a class="btn-floating btn-large blue darken-2">
    <i class="large material-icons">menu</i>
  </a>
  <ul>
    <li>
        <%= link_to event_paper_comments_path(@event, @paper, :format => :pdf), class: "btn-floating grey", target: "_blank" do %>
            <i class="material-icons tooltipped" data-position='left' data-tooltip='Generar informe'>file_download</i>
        <% end %>
    </li>
    <li>
        <%= link_to new_event_paper_review_path(@event, @paper), class: "btn-floating grey" do %>
            <i class="material-icons tooltipped" data-position='left' data-tooltip='Registrar revisión'>add_box</i>
        <% end %>
    </li>
  </ul>
</div>
<br>
<%= render 'papers/submission', paper: @paper %>
<%= render 'reviews/summary' %><br>
<%= render @paper.reviews %>
<%= render @paper.comments %><br>
<%= render 'comments/form' %>
